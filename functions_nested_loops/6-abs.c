#include "main.h"

/**
 * _abs - returns the absolute value 
 * @n: number passed to function
 *
 * Return: the absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);

	}
}
